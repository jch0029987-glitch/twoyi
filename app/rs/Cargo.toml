[dependencies]
jni = "0.21.1"
log = "0.4"
android_logger = "0.13"
libc = "0.2"
once_cell = "1.18"
unix_socket = "0.5.0"
# Changed 'event' to 'input' which is the correct feature for MotionEvent in v0.8
ndk = { version = "0.8.0", features = ["input", "native_window"] }
ndk-sys = "0.5.0"
